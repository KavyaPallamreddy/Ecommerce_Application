#Deployment
###controllers/pb-deploy.tml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce_application-deployment
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
  minReadySeconds: 5
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: ecommerce_application
  template:
    metadata:
      labels:
        app: 
    spec:
      containers:
      - name: ecommerce_application-container
        image: kavyapallamreddy/ecommerce_application:1
        imagePullPolicy: Always
        resources:
          limits:
            cpu: '1'
            memory: '500Mi'
          requests:
            cpu: '1'
            memory: '500Mi'
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: regcred



